# Specialized-Agents-in-M365-Copilot

A lightweight workshop-style app for exploring lab content with guided steps, outcomes, and progress tracking.

## Run locally

From the repository root, start a simple local server:

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000/index.html` in your browser.